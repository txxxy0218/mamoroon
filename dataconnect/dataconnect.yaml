specVersion: "v1beta"
serviceId: "mamoroom"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "mamoroom-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
